// importando a biblioteca padrão de entrada e saída de dados
// std(standard) i(input) o(output)
#include <stdio.h>
// Inicio do programa.
int main(){
// Comando para exibir o texto hello na tela.
// O comando printf só executa porque você fez a importação da biblioteca stdio.
    printf("Hello World\n");

// O comando abaixo imforma ao compilador que o programa acabou
    return 0;

}